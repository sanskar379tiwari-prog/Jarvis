\# Jarvis

A local AI agent that converts natural language into structured, validated file and system actions using a layered pipeline (intent → plan → execute) with lightweight context and control.

